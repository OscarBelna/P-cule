import { renderDashboard } from './DashboardRenderer.js';

/**
 * Initialise le tableau de bord
 */
export function initDashboard() {
    // Le dashboard n'a pas besoin d'initialisation spécifique
    // Toute la logique est dans le rendu
}

